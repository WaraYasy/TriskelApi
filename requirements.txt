# ============================================================
# Triskel API - Dependencias Completas
# ============================================================
# Incluye tanto la API REST (FastAPI) como el Dashboard Web (Flask)
#
# Instalación:
#   pip install -r requirements.txt
#
# ============================================================

# ==================== API REST (FastAPI) ====================
fastapi==0.109.0
uvicorn[standard]==0.40.0
pydantic-settings==2.1.0
email-validator==2.1.0              # Validación de emails para Pydantic

# ==================== Base de Datos ====================
firebase-admin==6.4.0            # Firestore (NoSQL)
sqlalchemy==2.0.25               # ORM para SQL (PostgreSQL, MySQL, MariaDB)
psycopg2-binary==2.9.9           # Driver para PostgreSQL (RECOMENDADO en Railway)

# ==================== Dashboard Web (Flask) ====================
flask==3.0.0                     # Framework web
httpx==0.25.2                    # Cliente HTTP para consumir API REST
pandas==2.1.4                    # Procesamiento de datos
plotly==5.18.0                   # Visualizaciones interactivas

# ==================== Configuración ====================
python-dotenv==1.0.0             # Variables de entorno

# ==================== Autenticación y Seguridad ====================
python-jose[cryptography]==3.3.0   # JWT token handling
passlib[bcrypt]==1.7.4             # Password hashing con bcrypt
bcrypt==4.0.1                        # Versión fijada compatible con passlib 1.7.4
python-multipart==0.0.6            # Form data parsing para login

# ==================== Producción ====================
gunicorn==21.2.0                 # WSGI server (producción)

# ==================== Testing ====================
pytest==7.4.3                    # Framework de testing
pytest-asyncio==0.21.1           # Testing async
pytest-cov==4.1.0                # Cobertura de código

# ==================== Opcional (Descomentar si se necesita) ====================
# flask-caching==2.1.0           # Sistema de caché
# flask-session==0.5.0           # Gestión de sesiones
# flask-login==0.6.3             # Sistema de login
# redis==5.0.1                   # Cache distribuido
# celery==5.3.4                  # Tareas asíncronas
