# ============================================================
# Triskel API - Variables de Entorno (Ejemplo)
# ============================================================
# Copia este archivo a .env y rellena con tus valores reales

# ==================== VARIABLES OBLIGATORIAS ====================

# Claves de Seguridad (OBLIGATORIAS)
# Genera claves seguras con: openssl rand -hex 32
SECRET_KEY=your-secret-key-here-change-this-in-production
API_KEY=your-api-key-here-change-this-in-production

# JWT Configuration (OBLIGATORIO para auth de administradores)
# Genera con: openssl rand -hex 64
JWT_SECRET_KEY=your-jwt-secret-key-here-change-this-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Password Hashing (opcional, valor por defecto: 12)
BCRYPT_ROUNDS=12

# Firebase (OBLIGATORIO)
# Para desarrollo local: usa el archivo de credenciales
# FIREBASE_CREDENTIALS_PATH=config/firebase-credentials.json (ya está por defecto)

# Para Railway/Producción: usa credenciales en base64
# Genera con: cat config/firebase-credentials.json | base64
# FIREBASE_CREDENTIALS_BASE64=paste-your-base64-encoded-firebase-credentials-here

# ==================== VARIABLES OPCIONALES ====================

# CORS Origins (opcional - solo necesario para Unity WebGL o Web)
# En desarrollo: permite todos los orígenes por defecto (*)
# En producción: especifica los orígenes permitidos separados por comas
#
# Ejemplos según tu caso:
# - Unity Nativo (Desktop/Android/iOS): NO necesitas configurar esto
# - Unity WebGL + Dashboard Web: CORS_ORIGINS=https://game.railway.app,https://dashboard.railway.app
# - Solo Dashboard Web: CORS_ORIGINS=https://dashboard.railway.app
#
# CORS_ORIGINS=https://tu-juego-webgl.com,https://tu-dashboard.com

# Log Level (opcional)
# Valores: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Por defecto: DEBUG en desarrollo, INFO en producción
# LOG_LEVEL=INFO

# ==================== Base de Datos SQL (PostgreSQL, MySQL, MariaDB) ====================
# Variables genéricas - Soporta PostgreSQL, MySQL, MariaDB cambiando solo las credenciales
# Para PostgreSQL (RECOMENDADO en Railway):
# DB_HOST=containers-us-west-123.railway.app
# DB_PORT=5432
# DB_NAME=railway
# DB_USER=postgres
# DB_PASSWORD=your_railway_postgres_password

# Para MySQL/MariaDB:
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=triskel_db
# DB_USER=triskel_user
# DB_PASSWORD=tu_password_seguro

# ==================== Redis (Rate Limiting - Producción) ====================
# OPCIONAL pero RECOMENDADO en producción
# Usado para rate limiting distribuido (múltiples instancias de la API)
# Si no está configurado, usa memoria local (funciona pero solo para 1 instancia)
#
# En Railway:
# 1. Agregar servicio Redis desde el template
# 2. Railway genera automáticamente REDIS_URL con el formato correcto
#
# Formato manual: redis://[usuario]:[password]@[host]:[port]/[db]
# REDIS_URL=redis://default:password@redis.railway.internal:6379/0

# ==================== NOTAS ====================
# Variables que NO necesitas configurar (se manejan automáticamente):
# - APP_NAME: hardcodeado como "Triskel-API"
# - DEBUG: detectado automáticamente según el entorno
# - PORT: proporcionado automáticamente por Railway ($PORT)
# - ENVIRONMENT: detectado automáticamente (production en Railway/Heroku, development en local)
