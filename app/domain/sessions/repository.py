"""
Repositorio para Sessions (acceso directo a Firestore)

TODO: Implementar operaciones CRUD de sesiones.

Arquitectura SIMPLE (sin ports).

Métodos sugeridos:
- create(session_data) -> GameSession
- get_by_id(session_id) -> Optional[GameSession]
- get_by_player(player_id) -> List[GameSession]
- get_by_game(game_id) -> List[GameSession]
- end_session(session_id, ended_at) -> GameSession
- get_active_session(player_id) -> Optional[GameSession]
"""

pass
